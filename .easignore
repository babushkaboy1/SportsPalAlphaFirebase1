# Node modules and logs (lockfiles should be included for reproducible builds)
node_modules/
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# Git and version control
.git
.gitignore

# Build artifacts
*.apk
*.aab
*.ipa
*.app
*.xcarchive
dist
build
web-build

# Expo caches
.expo/
.expo-shared/

# Test, docs, and local config
__tests__
test
tests
docs
coverage/
.env
.env.*

# Miscellaneous
*.log
.DS_Store
Thumbs.db

# Large or raw assets not needed for build
assets/raw/
assets/large/

# React Native native projects (managed workflow typically doesn’t include these)
android/
ios/

# Firebase Cloud Functions (not needed for client app builds)
functions/